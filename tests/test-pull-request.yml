name: Test PR Branch Name - Pull Request
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: pull_request
      GITHUB_REF: refs/pull/1/merge
      GITHUB_HEAD_REF: feature-branch
      GITHUB_BASE_REF: main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          repo-token: ${{ github.token }}
      - name: Assert branch output is set
        env:
          ACTUAL: ${{ steps.action.outputs.branch }}
        run: |
          echo "actual branch: $ACTUAL"
          if [ -z "$ACTUAL" ]; then
            echo "FAIL: expected branch output to be non-empty"
            exit 1
          fi
          echo "PASS: branch output is '$ACTUAL'"
